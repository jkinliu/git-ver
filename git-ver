Git版本管理（先从github上建立仓库，然后通过git clone取回到本地,在本地上添加代码，然后提交到远程仓库）

1，git init: 初始化仓库

2，git config --global user.name:配置提交者姓名

3, git config --global user.email:配置提交者邮箱

4，git add:增加到git仓库跟踪

5，git commit -m "first commit":提交代码改动

6，git clone：拷贝git项目

7, git log:查看git日志

8，git show（提交码）:查看git提交的具体修改

9，git checkout （-b) xxx:切换(新建)分支

10，git reset （提交码 --hard）:撤销提交(和修改）

11, git status：查看状态

12, git branch:查看分支

13，git remote add origin git.xxx.git
    git push -u origin master(xx)	远程仓库版本提交
